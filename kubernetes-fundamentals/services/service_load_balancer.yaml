apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend
  name: frontend-lb
  namespace: tamak
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: frontend
  type: LoadBalancer
status:
  loadBalancer: {}
